# ASR Server Python Dependencies
# Core dependencies (required)
numpy>=1.21.0
torch>=2.0.0
torchaudio>=2.0.0
transformers>=4.30.0
websockets>=11.0.0
omegaconf>=2.3.0
hydra-core>=1.3.0

# Primary ASR backend (may fail on some systems)
nemo-toolkit[asr]>=1.20.0
pytorch-lightning>=2.0.0

# Fallback ASR backend
openai-whisper>=20231117

# Optional dependencies
pyaudio>=0.2.11